# Zaman Pusulası — Environment Variables
# Copy this file to .env.local and fill in your values.

# Supabase (for auth and database — to be configured later)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# LLM API (for chatbot — to be configured later)
LLM_API_KEY=
LLM_MODEL=
